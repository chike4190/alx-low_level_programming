#include "main.h"
#include <stdio.h>
/**
 * _abs - computes the absolute value of an integer
 * @j: the integer to be checked
 * Return: the abs of the int
 */
int _abs(int j)
{
	if (j >= 0)
		return (j);
	else
		return (-j);
}
